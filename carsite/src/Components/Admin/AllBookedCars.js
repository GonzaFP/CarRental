import React from "react";
import { useSelector } from "react-redux";
import DataTable from "./DataTable";

function AllBookedCars() {
	return (
		<div>
			{" "}
			<DataTable />
		</div>
	);
}

export default AllBookedCars;
